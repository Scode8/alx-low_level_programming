#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: fist number
 * @b: second number
 * Return: Always (sum) (success)
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
